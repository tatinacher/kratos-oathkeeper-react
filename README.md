# Kratos + Oathkeeper + React

Registration flow

## Installation

```
    docker-compose -f docker-compose.yml up -d
```

## Install dependencies & Start React app

```
    yarn install
    yarn start
```
